# WebSSH Curated Snippets

Hand-curated shell snippets for the [WebSSH](https://webssh.net) app.
Browse them in-app: **Snippets → ••• → Import from Gist… → View our hand-curated snippets**.

Each collection below is a folder holding a `webssh.json` manifest and one file per snippet.
Want to share yours? See [CONTRIBUTING](CONTRIBUTING.md).

<!--
  Index format (parsed by the app — keep it simple):
    # Heading            → a themed section
    - [Title](collection-url) — subtitle `os` `os`
  The link must point to a collection folder of this directory
  (absolute /tree/master/snippets/... URL, or a bare folder name).
  Trailing backticked tokens are OS markers shown as filter chips.
  Everything else (prose, comments) is ignored by the parser.
-->

# Network

- [Network Essentials](https://github.com/isontheline/pro.webssh.net/tree/master/snippets/network) — Public IP, DNS and connectivity helpers `linux` `macos`
